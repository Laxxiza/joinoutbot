const privateCommand = require('./privateCommand');
const groupCommand = require('./groupCommand');
const initSession = require('./initSession');

module.exports = { privateCommand, groupCommand, initSession };