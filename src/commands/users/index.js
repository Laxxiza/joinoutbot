const onLine = require('./onLine');
const offLine = require('./offLine');
const timeOut = require('./timeOut');
const dinner = require('./dinner');
const fakename = require('./fakename');

module.exports = { onLine, offLine, timeOut, dinner, fakename };