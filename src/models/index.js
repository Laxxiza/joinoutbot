const Admin = require('./Admin');
const User = require('./User');
const Statistics = require('./Statistics');
const Channel = require('./Channel');

module.exports = { Admin, User, Channel, Statistics };